$("#rentalFormWrapper").html("<%= j render(partial: 'form', :locals => {reserveLater: @reserveLater, reservation: @reservation, availableToday: @availableToday})%> ");
$(".unavailableWrapper").hide()
updateDueDate();

