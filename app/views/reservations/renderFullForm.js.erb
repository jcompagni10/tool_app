$(".mainContent").html("<%= j render(partial: 'mainContent', :locals => {reservation: @reservation})%> ");
renderFullForm(@delivery)
mountDatePicker(@reservation.start_date);
mountStripe();
<% if @errors %>
    scrollTo("#page1");
<% end %>

