$(".mainContent").html("<%= j render(partial: 'form', :locals => {reservation: @reservation})%> ");
documentLoaded({isReload: true});
mountDatePicker("<%= @reservation.start_date %>")
renderFullForm(<%= @delivery %>);
<% if @errors %>
    scrollTo("#errorsWrapper");
    console.log("scroll")
<% end %>

